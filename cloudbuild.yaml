steps:
  - name: 'quarkus/quarkusio-build'
    args: [ 'build', '-Dquarkus.container-image.build=true', '-Dquarkus.container-image.name=precise-ledger-pro' ]
images:
  - 'gcr.io/$PROJECT_ID/precise-ledger-pro'
options:
  logging: CLOUD_LOGGING_ONLY